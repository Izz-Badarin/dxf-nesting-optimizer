"""Side-by-side comparison of nesting strategies (milestone M5)."""

from __future__ import annotations

from ..nesting.base import NestingResult


def compare_results(results: list[NestingResult]) -> dict[str, object]:
    """Build the comparison table and recommendation for two or more results."""
    raise NotImplementedError("comparison lands with milestone M5")
